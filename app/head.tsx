import { DefaultTags } from '#/components/DefaultTags'

const Head = () => (
  <>
    <DefaultTags />
    <title>Mixpoint</title>
    <meta
      name="description"
      content="Mixpoint is multi-track audio mixing app for the browser"
    />
  </>
)

export default Head
