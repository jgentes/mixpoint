name: Rome Linter
on: [pull_request]
jobs:
  linting-with-rome:
    runs-on: ubuntu-latest
    steps:
      - uses: rome/setup-rome@v0.4
        with:
          version: latest
      - uses: actions/checkout@v2
      - run: rome ci .
